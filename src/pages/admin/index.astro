---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// Get all data for dashboard overview
const leads = await getCollection('leads').catch(() => []);
const residents = await getCollection('residents').catch(() => []);
const facilities = await getCollection('facilities').catch(() => []);
const staff = await getCollection('staff').catch(() => []);

// Calculate key metrics
const newLeads = leads.filter(lead => lead.data.status === 'new').length;
const activeResidents = residents.filter(resident => resident.data.status === 'active').length;
const totalFacilities = facilities.length;
const activeStaff = staff.filter(member => member.data.status === 'active').length;

// Calculate occupancy rate
const totalCapacity = facilities.reduce((sum, facility) => sum + (facility.data.capacity || 0), 0);
const occupancyRate = totalCapacity > 0 ? Math.round((activeResidents / totalCapacity) * 100) : 0;

// Recent activity (mock data for demo)
const recentActivity = [
  { type: 'lead', message: 'New admission inquiry from Sarah M.', time: '2 hours ago' },
  { type: 'resident', message: 'John D. completed Phase 2 requirements', time: '4 hours ago' },
  { type: 'test', message: 'Drug test scheduled for Mike S.', time: '6 hours ago' },
  { type: 'payment', message: 'Payment received from Robert T.', time: '1 day ago' },
  { type: 'staff', message: 'Emily R. added case note for Alex M.', time: '1 day ago' }
];
---

<BaseLayout title="CRM Dashboard | NewBridge Living">
  <!-- Professional CRM Header -->
  <section class="bg-gradient-to-r from-emerald-900 to-emerald-800 py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-3xl font-bold text-white">🏠 NewBridge Living CRM</h1>
          <p class="text-emerald-200 mt-2">Complete sober living management system</p>
        </div>
        <div class="flex items-center space-x-4">
          <div class="bg-emerald-700 rounded-lg px-4 py-2">
            <p class="text-emerald-200 text-sm">Today</p>
            <p class="text-white font-bold">{new Date().toLocaleDateString()}</p>
          </div>
          <button class="bg-white text-emerald-800 px-6 py-3 rounded-lg font-semibold hover:bg-emerald-50 transition-all duration-300 flex items-center">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            Quick Add
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- Key Metrics Dashboard -->
  <section class="bg-white py-8 border-b">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-5 gap-6">
        <!-- New Leads -->
        <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-2xl p-6 border border-blue-200/50">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-blue-600 font-semibold text-sm uppercase tracking-wide">New Leads</p>
              <p class="text-3xl font-bold text-blue-700">{newLeads}</p>
              <p class="text-blue-600 text-sm">This week</p>
            </div>
            <div class="bg-blue-500 rounded-full p-3">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
            </div>
          </div>
        </div>

        <!-- Active Residents -->
        <div class="bg-gradient-to-br from-green-50 to-green-100 rounded-2xl p-6 border border-green-200/50">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-green-600 font-semibold text-sm uppercase tracking-wide">Active Residents</p>
              <p class="text-3xl font-bold text-green-700">{activeResidents}</p>
              <p class="text-green-600 text-sm">Current census</p>
            </div>
            <div class="bg-green-500 rounded-full p-3">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
              </svg>
            </div>
          </div>
        </div>

        <!-- Occupancy Rate -->
        <div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-2xl p-6 border border-purple-200/50">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-purple-600 font-semibold text-sm uppercase tracking-wide">Occupancy Rate</p>
              <p class="text-3xl font-bold text-purple-700">{occupancyRate}%</p>
              <p class="text-purple-600 text-sm">{activeResidents}/{totalCapacity} beds</p>
            </div>
            <div class="bg-purple-500 rounded-full p-3">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
              </svg>
            </div>
          </div>
        </div>

        <!-- Facilities -->
        <div class="bg-gradient-to-br from-orange-50 to-orange-100 rounded-2xl p-6 border border-orange-200/50">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-orange-600 font-semibold text-sm uppercase tracking-wide">Facilities</p>
              <p class="text-3xl font-bold text-orange-700">{totalFacilities}</p>
              <p class="text-orange-600 text-sm">Active locations</p>
            </div>
            <div class="bg-orange-500 rounded-full p-3">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
              </svg>
            </div>
          </div>
        </div>

        <!-- Staff Members -->
        <div class="bg-gradient-to-br from-teal-50 to-teal-100 rounded-2xl p-6 border border-teal-200/50">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-teal-600 font-semibold text-sm uppercase tracking-wide">Staff Members</p>
              <p class="text-3xl font-bold text-teal-700">{activeStaff}</p>
              <p class="text-teal-600 text-sm">Active team</p>
            </div>
            <div class="bg-teal-500 rounded-full p-3">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"></path>
              </svg>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Main Dashboard Content -->
  <section class="bg-gray-50 py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        <!-- CRM Module Navigation -->
        <div class="lg:col-span-2">
          <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
            <h3 class="text-xl font-bold text-gray-900 mb-6">CRM Modules</h3>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <!-- Leads Management -->
              <a href="/admin/leads" class="group bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-6 border border-blue-200/50 hover:shadow-lg transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                  <div class="bg-blue-500 rounded-lg p-3">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                    </svg>
                  </div>
                  <span class="text-blue-600 group-hover:text-blue-700 font-bold text-2xl">{newLeads}</span>
                </div>
                <h4 class="font-bold text-blue-900 mb-2">Leads Management</h4>
                <p class="text-blue-700 text-sm">Track admissions inquiries and conversions</p>
              </a>

              <!-- Residents Management -->
              <a href="/admin/residents" class="group bg-gradient-to-br from-green-50 to-green-100 rounded-xl p-6 border border-green-200/50 hover:shadow-lg transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                  <div class="bg-green-500 rounded-lg p-3">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                    </svg>
                  </div>
                  <span class="text-green-600 group-hover:text-green-700 font-bold text-2xl">{activeResidents}</span>
                </div>
                <h4 class="font-bold text-green-900 mb-2">Residents Management</h4>
                <p class="text-green-700 text-sm">Manage current residents and progress tracking</p>
              </a>

              <!-- Compliance & Drug Testing -->
              <a href="/admin/compliance" class="group bg-gradient-to-br from-purple-50 to-purple-100 rounded-xl p-6 border border-purple-200/50 hover:shadow-lg transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                  <div class="bg-purple-500 rounded-lg p-3">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                    </svg>
                  </div>
                  <span class="text-purple-600 group-hover:text-purple-700 font-bold text-2xl">94%</span>
                </div>
                <h4 class="font-bold text-purple-900 mb-2">Compliance & Testing</h4>
                <p class="text-purple-700 text-sm">Drug testing, medications, and compliance tracking</p>
              </a>

              <!-- Staff Management -->
              <a href="/admin/staff" class="group bg-gradient-to-br from-teal-50 to-teal-100 rounded-xl p-6 border border-teal-200/50 hover:shadow-lg transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                  <div class="bg-teal-500 rounded-lg p-3">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"></path>
                    </svg>
                  </div>
                  <span class="text-teal-600 group-hover:text-teal-700 font-bold text-2xl">{activeStaff}</span>
                </div>
                <h4 class="font-bold text-teal-900 mb-2">Staff Management</h4>
                <p class="text-teal-700 text-sm">Manage staff, roles, and permissions</p>
              </a>

              <!-- Financial Management -->
              <a href="/admin/financial" class="group bg-gradient-to-br from-yellow-50 to-yellow-100 rounded-xl p-6 border border-yellow-200/50 hover:shadow-lg transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                  <div class="bg-yellow-500 rounded-lg p-3">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                    </svg>
                  </div>
                  <span class="text-yellow-600 group-hover:text-yellow-700 font-bold text-2xl">$24k</span>
                </div>
                <h4 class="font-bold text-yellow-900 mb-2">Financial Management</h4>
                <p class="text-yellow-700 text-sm">Rent tracking, payments, and financial reports</p>
              </a>

              <!-- Case Notes -->
              <a href="/admin/case-notes" class="group bg-gradient-to-br from-indigo-50 to-indigo-100 rounded-xl p-6 border border-indigo-200/50 hover:shadow-lg transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                  <div class="bg-indigo-500 rounded-lg p-3">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                  </div>
                  <span class="text-indigo-600 group-hover:text-indigo-700 font-bold text-2xl">127</span>
                </div>
                <h4 class="font-bold text-indigo-900 mb-2">Case Notes</h4>
                <p class="text-indigo-700 text-sm">Clinical notes, progress tracking, and incidents</p>
              </a>
            </div>
          </div>
        </div>

        <!-- Sidebar -->
        <div class="space-y-6">
          
          <!-- Recent Activity -->
          <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
            <h3 class="text-lg font-bold text-gray-900 mb-4">Recent Activity</h3>
            <div class="space-y-4">
              {recentActivity.map(activity => (
                <div class="flex items-start">
                  <div class={`rounded-full p-2 mr-3 ${
                    activity.type === 'lead' ? 'bg-blue-100' :
                    activity.type === 'resident' ? 'bg-green-100' :
                    activity.type === 'test' ? 'bg-purple-100' :
                    activity.type === 'payment' ? 'bg-yellow-100' :
                    'bg-gray-100'
                  }`}>
                    <div class={`w-2 h-2 rounded-full ${
                      activity.type === 'lead' ? 'bg-blue-500' :
                      activity.type === 'resident' ? 'bg-green-500' :
                      activity.type === 'test' ? 'bg-purple-500' :
                      activity.type === 'payment' ? 'bg-yellow-500' :
                      'bg-gray-500'
                    }`}></div>
                  </div>
                  <div class="flex-1">
                    <p class="text-sm text-gray-900">{activity.message}</p>
                    <p class="text-xs text-gray-500">{activity.time}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>

          <!-- Quick Actions -->
          <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
            <h3 class="text-lg font-bold text-gray-900 mb-4">Quick Actions</h3>
            <div class="space-y-3">
              <button class="w-full bg-emerald-600 hover:bg-emerald-700 text-white px-4 py-3 rounded-lg font-semibold transition-all duration-300 flex items-center justify-center text-sm">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                Add New Lead
              </button>
              
              <button class="w-full bg-blue-600 hover:bg-blue-700 text-white px-4 py-3 rounded-lg font-semibold transition-all duration-300 flex items-center justify-center text-sm">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path>
                </svg>
                Log Drug Test
              </button>
              
              <button class="w-full bg-purple-600 hover:bg-purple-700 text-white px-4 py-3 rounded-lg font-semibold transition-all duration-300 flex items-center justify-center text-sm">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
                Add Case Note
              </button>

              <button class="w-full bg-yellow-600 hover:bg-yellow-700 text-white px-4 py-3 rounded-lg font-semibold transition-all duration-300 flex items-center justify-center text-sm">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                </svg>
                Record Payment
              </button>
            </div>
          </div>

          <!-- System Status -->
          <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
            <h3 class="text-lg font-bold text-gray-900 mb-4">System Status</h3>
            <div class="space-y-3">
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">Database</span>
                <div class="flex items-center">
                  <div class="w-2 h-2 bg-green-500 rounded-full mr-2"></div>
                  <span class="text-xs text-green-600 font-semibold">Online</span>
                </div>
              </div>
              
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">Compliance Reports</span>
                <div class="flex items-center">
                  <div class="w-2 h-2 bg-green-500 rounded-full mr-2"></div>
                  <span class="text-xs text-green-600 font-semibold">Current</span>
                </div>
              </div>
              
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">Data Backup</span>
                <div class="flex items-center">
                  <div class="w-2 h-2 bg-yellow-500 rounded-full mr-2"></div>
                  <span class="text-xs text-yellow-600 font-semibold">2 hrs ago</span>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>
    </div>
  </section>

  <!-- JavaScript for Dashboard Interactivity -->
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Auto-refresh dashboard stats every 5 minutes
      setInterval(() => {
        console.log('Refreshing dashboard data...');
        // In production, this would fetch updated stats from your API
      }, 300000); // 5 minutes
      
      // Quick action handlers
      const quickActions = document.querySelectorAll('[class*="bg-emerald-600"], [class*="bg-blue-600"], [class*="bg-purple-600"], [class*="bg-yellow-600"]');
      
      quickActions.forEach(button => {
        button.addEventListener('click', function() {
          const buttonText = this.textContent.trim();
          
          // Route to appropriate module based on button text
          if (buttonText.includes('Lead')) {
            window.location.href = '/admin/leads';
          } else if (buttonText.includes('Drug Test')) {
            window.location.href = '/admin/compliance';
          } else if (buttonText.includes('Case Note')) {
            window.location.href = '/admin/case-notes';
          } else if (buttonText.includes('Payment')) {
            window.location.href = '/admin/financial';
          }
        });
      });
      
      console.log('✅ NewBridge Living CRM Dashboard loaded successfully');
    });
  </script>
</BaseLayout>